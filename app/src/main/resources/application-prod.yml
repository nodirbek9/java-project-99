spring:
  application:
    name: app
  sql:
    init:
      platform: postgres
  datasource:
    url: jdbc:postgresql://localhost:5432/aml
    driver-class-name: org.postgresql.Driver
    username: postgres
    password: 1111
  jpa:
    hibernate:
      ddl-auto: update

server:
  port: 8080